import React from 'react';

const Loading = () => {
    return (
        <div className='mt-24'>
            <span className="loading loading-dots loading-lg"></span>
        </div>
    );
};

export default Loading;